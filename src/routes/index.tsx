import { createFileRoute, useNavigate } from "@tanstack/react-router";
// TEMP marker — will be replaced by full craft in next commit
export const Route = createFileRoute("/")({ component: () => null });
